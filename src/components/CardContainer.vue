<script setup>
    import CarCard from './CarCard.vue'
</script>
<template>
     <div class="card-container">

        <!-- massiv.map(item => {
            return (
                <CarCard item.src item/>
            )
        }) -->
        <CarCard/>
        <CarCard/>
        <CarCard/>
        <CarCard/>
</div>
</template>
